# Mewo folder (`.mewo`)

Every project has a Mewo folder (`.mewo`). It's rather empty right now.

## Files

### `project.json`

There are only 2 fields right now.

- `schema_version`: should be `1`.
- `created_at`: Unix timestamp.
